# MySSH - 远程服务器运维工具

一个基于 Tauri + Vue 3 开发的现代化 SSH 运维工具，类似 XShell 的多服务器管理工具。

## ✨ 功能特性

### 🔐 服务器管理
- ✅ 多服务器配置管理（支持密码和密钥认证）
- ✅ 服务器配置持久化存储（SQLite）
- ✅ 快速连接/断开服务器
- ✅ 服务器状态实时显示

### 💻 终端功能
- ✅ 多标签页终端支持
- ✅ 终端分屏（水平/垂直）
- ✅ 命令历史记录
- ✅ 终端录制与回放
- ✅ 自定义主题和字体大小
- ✅ 清屏和重连功能

### 📁 文件管理
- ✅ 远程文件浏览
- ✅ 文件上传/下载（支持多文件）
- ✅ 文件/目录创建、删除、重命名
- ✅ 文件权限管理（chmod）
- ✅ 文件预览和比较
- ✅ 拖拽上传支持

### 📊 系统监控
- ✅ 实时 CPU 使用率监控
- ✅ 内存使用情况
- ✅ 磁盘空间监控
- ✅ 网络流量统计
- ✅ 自动刷新功能

### 🤖 AI 助手
- ✅ 智能对话助手
- ✅ 快速操作建议
- ✅ 上下文感知

### 🎨 界面特性
- ✅ 现代化暗色主题
- ✅ 响应式布局设计
- ✅ 可调整的面板大小
- ✅ 快捷键支持
- ✅ 全局提示消息

## 🛠️ 技术栈

### 前端
- **Vue 3** - 渐进式 JavaScript 框架
- **Pinia** - 状态管理
- **Vue Router** - 路由管理
- **Vite** - 构建工具
- **Tauri API** - 与后端通信

### 后端
- **Rust** - 系统编程语言
- **Tauri** - 轻量级桌面应用框架
- **SQLite** - 嵌入式数据库（rusqlite）
- **serde** - 序列化/反序列化

## 📦 安装

### 环境要求

- **Node.js** >= 16.0.0
- **Rust** >= 1.60.0
- **系统依赖**：
  - macOS: Xcode Command Line Tools
  - Linux: `libwebkit2gtk-4.0-dev`, `build-essential`, `curl`, `wget`, `libssl-dev`, `libgtk-3-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`
  - Windows: Microsoft Visual Studio C++ Build Tools

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd myssh
```

2. **安装前端依赖**
```bash
npm install
```

3. **安装 Rust 依赖**（首次运行会自动安装）
```bash
cd src-tauri
cargo build
```

## 🚀 运行

### 开发模式

```bash
# 启动开发服务器
npm run tauri dev
```

这将同时启动：
- Vite 开发服务器（前端）：`http://localhost:1420`
- Tauri 应用窗口

### 生产构建

```bash
# 构建应用
npm run tauri build
```

构建产物位于 `src-tauri/target/release/` 目录。

## 📁 项目结构

```
myssh/
├── src/                      # 前端源代码
│   ├── api/                  # Tauri API 调用封装
│   │   ├── ssh.js           # SSH 相关 API
│   │   ├── file.js          # 文件管理 API
│   │   ├── monitor.js       # 系统监控 API
│   │   ├── server.js        # 服务器配置 CRUD API
│   │   └── ai.js            # AI 助手 API
│   ├── components/           # Vue 组件
│   │   ├── ServerList.vue   # 服务器列表
│   │   ├── TerminalTab.vue  # 终端标签页
│   │   ├── FileManagerTab.vue # 文件管理
│   │   ├── MonitorTab.vue  # 系统监控
│   │   └── AIChatTab.vue    # AI 助手
│   ├── stores/              # Pinia 状态管理
│   │   └── serverStore.js   # 服务器状态
│   ├── views/               # 页面视图
│   │   └── MainLayout.vue  # 主布局
│   ├── utils/               # 工具函数
│   ├── router/              # 路由配置
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── src-tauri/               # Tauri 后端
│   ├── src/
│   │   ├── main.rs         # 主入口
│   │   ├── db.rs           # 数据库管理
│   │   ├── ssh.rs          # SSH 连接处理
│   │   ├── file.rs         # 文件管理处理
│   │   ├── monitor.rs      # 系统监控处理
│   │   ├── server.rs       # 服务器配置 CRUD
│   │   └── ai.rs           # AI 助手处理
│   ├── Cargo.toml          # Rust 依赖配置
│   └── tauri.conf.json     # Tauri 配置
├── package.json            # Node.js 依赖配置
├── vite.config.js          # Vite 配置
└── README.md               # 本文件
```

## 🔌 API 接口

### 服务器管理
- `get_servers()` - 获取所有服务器配置
- `save_server(server)` - 保存服务器配置
- `update_server(server)` - 更新服务器配置
- `delete_server(serverId)` - 删除服务器配置
- `get_server(serverId)` - 获取单个服务器配置

### SSH 连接
- `connect_ssh_server(params)` - 连接 SSH 服务器
- `disconnect_ssh_server(serverId)` - 断开连接
- `execute_ssh_command(params)` - 执行 SSH 命令
- `reconnect_terminal(serverId)` - 重连终端

### 文件管理
- `list_remote_directory(params)` - 获取远程目录文件列表
- `upload_file(params)` - 上传文件
- `download_file(params)` - 下载文件
- `create_directory(params)` - 创建目录
- `delete_files(params)` - 删除文件/目录
- `rename_file(params)` - 重命名文件
- `change_file_mode(params)` - 修改文件权限

### 系统监控
- `get_system_monitor(serverId)` - 获取系统监控数据

### AI 助手
- `chat_with_ai(params)` - 与 AI 对话
- `get_ai_quick_actions(serverId)` - 获取快速操作建议

详细 API 文档请参考 `src/api/` 目录下的文件。

## 💾 数据存储

项目使用 **SQLite** 数据库存储服务器配置，数据库文件位置：

- **macOS**: `~/Library/Application Support/myssh/myssh.db`
- **Windows**: `%APPDATA%\myssh\myssh.db`
- **Linux**: `~/.local/share/myssh/myssh.db`

### 数据库表结构

**servers 表**：
- `id` (TEXT, PRIMARY KEY) - 服务器ID
- `name` (TEXT) - 服务器名称
- `host` (TEXT) - 主机地址
- `port` (INTEGER) - 端口号
- `username` (TEXT) - 用户名
- `password` (TEXT, NULLABLE) - 密码
- `key_path` (TEXT, NULLABLE) - 密钥路径
- `created_at` (TEXT) - 创建时间
- `updated_at` (TEXT) - 更新时间

## ⌨️ 快捷键

- `Ctrl/Cmd + N` - 添加新服务器
- `Ctrl/Cmd + W` - 关闭当前服务器标签页
- `Ctrl/Cmd + F` - 聚焦搜索框（文件管理器）

## 🎨 界面说明

### 布局结构

```
┌─────────────────────────────────────────┐
│  服务器列表  │  主工作区                │
│             │  ┌─────────────────────┐ │
│  - 服务器1  │  │  服务器标签页        │ │
│  - 服务器2  │  └─────────────────────┘ │
│             │  ┌──────────┬──────────┐ │
│             │  │  终端    │  文件管理 │ │
│             │  │          │          │ │
│             │  └──────────┴──────────┘ │
│             │  ┌─────────────────────┐ │
│             │  │  AI助手/系统监控    │ │
│             │  └─────────────────────┘ │
└─────────────────────────────────────────┘
```

### 响应式设计

- 支持窗口大小调整
- 小屏幕自动适配布局
- 所有元素在小窗口下可见
- 面板大小可拖动调整

## 🔧 开发指南

### 添加新的 API 接口

1. **前端**：在 `src/api/` 目录下创建或修改对应的 API 文件
2. **后端**：在 `src-tauri/src/` 目录下创建或修改对应的 Rust 模块
3. **注册**：在 `src-tauri/src/main.rs` 中注册新的命令

### 数据库迁移

当需要修改数据库结构时：

1. 在 `src-tauri/src/db.rs` 的 `migrate_database()` 函数中添加迁移逻辑
2. 更新 `schema_version` 表记录版本号

### 代码规范

- **前端**：遵循 Vue 3 Composition API 风格
- **后端**：遵循 Rust 官方代码规范
- **错误处理**：所有 API 调用都应该有错误处理

## 🐛 故障排除

### 编译错误

如果遇到编译错误：

```bash
cd src-tauri
cargo clean
cargo build
```

### 数据库问题

如果数据库文件损坏，可以删除数据库文件，应用会自动重新创建。

### 端口占用

如果开发服务器端口 1420 被占用，可以修改 `vite.config.js` 中的端口配置。

## 📝 待实现功能

- [ ] SSH 密钥管理
- [ ] 文件传输进度显示
- [ ] 终端会话持久化
- [ ] 多窗口支持
- [ ] 主题切换
- [ ] 插件系统
- [ ] 命令模板
- [ ] 日志导出

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

[待定]

## 🙏 致谢

- [Tauri](https://tauri.app/) - 优秀的桌面应用框架
- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [SQLite](https://www.sqlite.org/) - 轻量级数据库

---

**注意**：当前版本为开发阶段，部分功能仍在实现中。后端 SSH 连接、文件传输等功能需要实现具体的业务逻辑。

